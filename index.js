class Human {
    static isLivingonDeath = true;

    constructor(name, address) {
        this.name = name;
        this.address = address;
    }

    introduce(){
        console.log(`Hi, my name is ${this.name}`)
    }
}

let person = new Human("rakha", "binong");

person.

console.log(person.introduce());